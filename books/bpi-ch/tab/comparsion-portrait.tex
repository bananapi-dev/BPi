\begin{table}[ht]
\caption{package operations using different package managers} % title of Table
\centering  % used for centering table
\begin{tabular}{c c c c c c c} % centered columns (7 columns)
\hline\hline                        %inserts double horizontal lines
Package Manager & query & install & remove & deb & windows & OSX \\ [0.5ex] % inserts table 
%heading
\hline                 % inserts single horizontal line
rpm   & rpm -qil hello.rpm  & rpm -ivh hello.rpm & rpm -e hello  & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec  \\ [0.5ex] % inserts table 
rpm   & rpm -qil hello.rpm  & rpm -ivh hello.rpm & rpm -e hello  & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec  \\ [0.5ex] % inserts table 
rpm   & rpm -qil hello.rpm  & rpm -ivh hello.rpm & rpm -e hello  & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec  \\ [0.5ex] % inserts table 
rpm   & rpm -qil hello.rpm  & rpm -ivh hello.rpm & rpm -e hello  & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec  \\ [0.5ex] % inserts table 
rpm   & rpm -qil hello.rpm  & rpm -ivh hello.rpm & rpm -e hello  & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec  \\ [0.5ex] % inserts table 
rpm   & rpm -qil hello.rpm  & rpm -ivh hello.rpm & rpm -e hello  & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec  \\ [0.5ex] % inserts table 
rpm   & rpm -qil hello.rpm  & rpm -ivh hello.rpm & rpm -e hello  & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec & rpmbuild -ba hello.spec  \\ [0.5ex] % inserts table 

\hline %inserts single line
\end{tabular}
\label{table:comparsion-portrait} % is used to refer this table in the text
%\end{table}
