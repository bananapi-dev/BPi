# Refresh package lists with repostiory server
sudo apt-get update 
# Install xrdp and Ubuntu Desktop
sudo apt-get install ubuntu-desktop xrdp -y
# Install korn shell
sudo apt-get install ksh 
#install chrome
#install gsutil certificate
